{
  "name": "EDAGrads",
  "version": "1.0.0",
  "description": "it's a real sweet app",
  "main": "index.js",
  "scripts": {
    "start": "budo . -d --serve bundle.js --live",
    "build": "browserify . -o bundle.js -g uglifyify",
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "browserify test/hello.js | tape-run | tap-spec",
    "test:watch": "watchify test/hello.js -o '| tape-run' ",
    "test:debug": "budo test/hello.js -d --serve bundle.js --live ",
    "dist": "mkdir -p dist && bash -c 'cp *.{js,html,css} dist'",
    "push": "gh-pages -d dist",
    "deploy": "npm run build && npm run dist && npm run push"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": "es2015"
        }
      ],
      [
        "brfs"
      ]
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pietgeursen/"
  },
  "author": "pietgeursen",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/pietgeursen//issues"
  },
  "homepage": "https://github.com/pietgeursen/",
  "dependencies": {},
  "devDependencies": {
    "babel-preset-es2015": "^6.14.0",
    "babelify": "^7.3.0",
    "brfs": "^1.4.3",
    "browserify": "^13.1.0",
    "budo": "^8.2.0",
    "cuke-tap": "github:pietgeursen/cuke-tap",
    "dependency-check": "^2.6.0",
    "gh-pages": "^0.11.0",
    "inu": "^3.1.3",
    "inux": "^2.0.7",
    "jsdom": "^9.4.2",
    "pull-dom-mutants": "^1.0.0",
    "pull-test": "^1.2.3",
    "smokestack": "*",
    "tap-spec": "^4.1.1",
    "tape": "^4.5.1",
    "tape-run": "^2.1.4",
    "uglifyify": "^3.0.1",
    "watchify": "^3.7.0"
  }
}
